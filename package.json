{
  "name": "@cuny-ai-lab/cail-sandbox-client",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "packageManager": "bun@1.3.5",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./contract/sandbox-openapi.json": "./contract/sandbox-openapi.json"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "contract",
    "dist",
    "src",
    "vendor",
    "CONTRACT.md",
    "README.md"
  ],
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "bun run check:service-contract && bun test",
    "check": "bun run check:service-contract && bun run typecheck && bun test && bun run build",
    "check:service-contract": "bun run scripts/check-service-contract.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "bun run check && git diff --exit-code -- dist",
    "prepare": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "eventsource-parser": "3.1.0"
  }
}
