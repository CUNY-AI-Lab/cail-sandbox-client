import { expect,test } from "bun:test"; import { CailSandboxError,createCailSandboxClient } from "../src/index";
const jwt={kind:"jwt" as const,token:"session-token"};
test("owns exactly one CAIL credential and app header",async()=>{let seen!:Request;const c=createCailSandboxClient({baseUrl:"https://x",app:"kale-workbench",fetchImpl:async(i,n)=>{seen=new Request(i,n);return Response.json({id:"x",state:"active",expires_at:"later"},{status:201})}});await c.create(jwt);expect(seen.headers.get("x-cail-identity-jwt")).toBe("session-token");expect(seen.headers.get("authorization")).toBeNull();expect(seen.headers.get("x-cail-app")).toBe("kale-workbench")});
test("preserves raw file response",async()=>{const original=new Response(new Uint8Array([0,255]),{headers:{"content-type":"application/octet-stream"}});const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>original});expect(await c.readFile("box","a.bin",jwt)).toBe(original)});
test("parses nested CAIL errors",async()=>{const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>Response.json({error:{message:"No.",type:"permission_error",param:null,code:"forbidden",cail:{scope:"sandbox:exec"}}},{status:403})});try{await c.running("x",jwt);throw new Error("missing") }catch(e){expect(e).toBeInstanceOf(CailSandboxError);expect((e as CailSandboxError).code).toBe("forbidden");expect((e as CailSandboxError).details).toEqual({scope:"sandbox:exec"})}});
test("streams decoded output and one terminal event",async()=>{const body='event: stdout\ndata: {"data":"aGk="}\n\nevent: exit\ndata: {"exit_code":0}\n\n';const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>new Response(body,{headers:{"content-type":"text/event-stream"}})});const out=[];for await(const e of await c.exec("box","echo hi",jwt))out.push(e);expect(out[0]).toEqual({type:"stdout",data:new TextEncoder().encode("hi")});expect(out[1]).toEqual({type:"exit",exitCode:0})});
test("uses standard SSE framing across CRLF and split chunks",async()=>{const encoder=new TextEncoder(),parts=['event: stdout\r\ndata: {"data":','"aGk="}\r\n\r\nevent: exit\r\ndata: {"exit_code":0}\r\n\r\n'];const stream=new ReadableStream({start(controller){for(const part of parts)controller.enqueue(encoder.encode(part));controller.close()}});const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>new Response(stream,{headers:{"content-type":"text/event-stream"}})});const out=[];for await(const e of await c.exec("box","echo hi",jwt))out.push(e);expect(out).toHaveLength(2);expect(out[1]).toEqual({type:"exit",exitCode:0})});
test("canceling iteration cancels the underlying command response",async()=>{let canceled=false;const bytes=new TextEncoder().encode('event: stdout\ndata: {"data":"eA=="}\n\n');const stream=new ReadableStream({start(controller){controller.enqueue(bytes)},cancel(){canceled=true}});const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>new Response(stream,{headers:{"content-type":"text/event-stream"}})});const events=await c.exec("box","long command",jwt);expect((await events.next()).value).toEqual({type:"stdout",data:new TextEncoder().encode("x")});await events.return(undefined);expect(canceled).toBeTrue()});
test("rejects unknown SSE event types consistently",async()=>{const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>new Response('event: heartbeat\ndata: not-json\n\n')});await expect((async()=>{for await(const _ of await c.exec("box","x",jwt))void _})()).rejects.toThrow("unknown event type")});
test("rejects stream without exactly one terminal event",async()=>{const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>new Response('event: stdout\ndata: {"data":"eA=="}\n\n')});await expect((async()=>{for await(const _ of await c.exec("box","x",jwt))void _})()).rejects.toThrow("without a terminal")});
test("rejects client-side path traversal",async()=>{const c=createCailSandboxClient({baseUrl:"https://x",app:"kale",fetchImpl:async()=>new Response()});await expect(c.readFile("box","../secret",jwt)).rejects.toThrow("workspace-relative")});
